<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F1F5F9">

    <!-- Toolbar Row -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="16dp"
        android:gravity="center_vertical">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="Workspaces"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#111827"/>

        <Button
            android:id="@+id/btnAddSpace"
            android:layout_width="wrap_content"
            android:layout_height="40dp"
            android:text="+ Add Space"
            android:textSize="13sp"
            android:textColor="#FFFFFF"
            android:backgroundTint="#2563EB"/>
    </LinearLayout>

    <ProgressBar
        android:id="@+id/progressBarSpaces"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"/>

    <TextView
        android:id="@+id/tvEmptySpaces"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="No workspaces found.\nTap '+ Add Space' to create one."
        android:textSize="15sp"
        android:textColor="#9CA3AF"
        android:textAlignment="center"
        android:padding="32dp"
        android:visibility="gone"
        android:lineSpacingMultiplier="1.5"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvAdminSpaces"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:padding="8dp"
        android:clipToPadding="false"/>

</LinearLayout>
